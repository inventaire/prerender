# Custom Prerender for [inventaire.io](https://inventaire.io)'s needs

## Install
```sh
git clone https://github.com/inventaire/prerender
cd prerender
npm install
npm
```

## Add to Systemd
```sh
npm run add-to-systemd
sudo systemctl start prerender
```
